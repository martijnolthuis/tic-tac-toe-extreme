// Importing necessary modules from Vue 3 library
import { createApp } from "vue";

// Importing the main App component from its file location
import App from "./App.vue";

// Creating a new Vue application instance using the App component
// and mounting it to the HTML element with the id of "app".
createApp(App).mount("#app");
